server:
  port: ${SERVER_PORT:8088}
  servlet:
    context-path: /
    session:
      timeout: 3600 # redefine default 30m by 1 hour

info:
  app:
    name: ti-sql-agent
    version: '@project.version@'

spring:
  application:
    name: ${info.app.name}
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://${OKTA_DOMAIN}/
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          batch_size: 30

  datasource:
    assistant:
      url: ${ASSISTANT_DB_URL:jdbc:postgresql://localhost:5434/assistant_db}
      username: ${ASSISTANT_USER:assistant_user}
      password: ${ASSISTANT_PASSWORD:qwerty}
      driver-class-name: org.postgresql.Driver
      hikari:
        minimum-idle: 5
        maximum-pool-size: 10
        connection-timeout: 50000
        leak-detection-threshold: 20000
        max-lifetime: 1800000
        idle-timeout: 600000
  ai:
    openai:
      api-key: ${OPEN_AI_API_KEY}
      #base-url: ${OPEN_AI_ENDPOINT:https://ai-proxy.lab.epam.com/openai/deployments/gpt-4.1-mini-2025-04-14}
      chat:
        model: "${CHAT_MODEL:gpt-4.1-mini-2025-04-14}" # gpt-4-turbo
        base-url: ${OPEN_AI_ENDPOINT:https://ai-proxy.lab.epam.com/openai/deployments/gpt-4.1-mini-2025-04-14}
        temperature: 0.7
    retry:
      max-attempts: 2
      backoff:
        initial-interval: 4000ms
      on-client-errors: true
    timeout: 40000ms

datasource:
  knowledge:
    url: ${KNOWLEDGE_DB_URL:jdbc:postgresql://localhost:5432/knowledge_db}
    username: ${KNOWLEDGE_USER:knowledge_user}
    password: ${KNOWLEDGE_PASSWORD:qwerty}
    driver-class-name: org.postgresql.Driver

ms:
  cors:
    allowed-origins: "${SWAGGER_ALLOWED_ORIGINS:http://localhost:8080}"

group:
  admin: ${ADMINS}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,zipkin
  endpoint:
    prometheus:
      access: read_only
  metrics:
    tags:
      application: ${spring.application.name}
    distribution:
      percentiles-histogram:
        http.server.requests: true
  tracing:
    sampling:
      probability: ${TRACING_SAMPLING_PROBABILITY:1.0}
    export:
      zipkin:
        enabled: true
        endpoint: ${MANAGEMENT_ZIPKIN_TRACING_ENDPOINT:http://localhost:9411/api/v2/spans}

logging:
  level:
    com.wk.ti: debug
    org.springframework: info
  # Enable Native JSON Structured Logging (Spring Boot 3.4+)
  structured:
    format:
      console: ecs # Emits logs in ECS JSON format containing service, traceId, spanId

springdoc:
  api-docs:
    path: /v3/api-docs


